---
title: Release Voice Flow v1.2.0
type: feat
status: active
date: 2026-09-05
---

## Overview and problem frame

Publish the approved dictation workspace and Vibe coding work to origin/master
and trigger the existing signed macOS release workflow with tag v1.2.0.

## Scope boundaries

Include the verified source changes, synchronized desktop/Rust/Tauri versions,
and changelog. Exclude generated capability schemas, TypeScript build state and
the local VSIX build artifact. No force push or tag replacement.

## Implementation units

- [x] Verify origin, current release and remote branch state.
- [x] Set desktop, Cargo, Cargo lock and Tauri versions to 1.2.0.
- [x] Run release configuration tests (19 passed) and diff whitespace checks.
- [ ] Commit reviewed source and push master and the new annotated tag.
- [ ] Confirm the release workflow started for that exact commit.

## System-wide impact

The tag triggers the existing universal macOS signing/notarization workflow.
Production publication occurs only after its build succeeds.

## Risks and dependencies

The GitHub credential cannot list repository secret names (HTTP 403); the
workflow preflight will validate their presence. The last three release runs
succeeded. No secret values were requested or exposed.

The native E2E runner provisions capabilities and replaces test settings.
Use the already completed native UI review and automated verification; skip
that optional hook step rather than changing capabilities during release.

## Verification evidence

Prior implementation: 923 Rust tests, 110 frontend tests, 6 editor adapter tests,
Clippy, formatting, typechecks, i18n and native bundle/UI inspection passed.
Release tests: 19 passed. Working diff passes whitespace checks.
