---
title: Release the smooth activity chart as v1.2.1
type: fix
status: active
date: 2026-09-05
---

## Scope

Publish the verified chart change through the existing tagged macOS release
workflow. Keep generated local artifacts outside the commit.

## Implementation units

- [x] Confirm v1.2.0 succeeded and origin master matches the local base.
- [x] Set desktop, Tauri and Cargo versions to 1.2.1 and update the changelog.
- [x] Verify frontend and release checks.
- [ ] Commit and push master with the annotated v1.2.1 tag.
- [ ] Confirm the release workflow started.

## Verification

Chart tests, TypeScript build and locale checks passed during implementation.
The rendered component was visually inspected using synthetic daily values.

Release verification: 111 frontend tests and 14 release contract tests passed.
Git whitespace validation passed.

The commit hook exposed a pre-existing async test race in PlatformQualityPage.
The assertion now waits for the rendered summary rather than only the mock call.
No product behavior was changed for this test correction.
